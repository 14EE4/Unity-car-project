# Development Log

이 문서는 날짜별 수정 기록과 주행 디버그 메모를 모아둔 파일입니다. 자세한 문제 해결 기록은 [Troubleshooting](TROUBLESHOOTING.md)도 함께 참고하세요.

## 2026-05-20

- Key Guide 버튼 중복/불일치 문제를 해결했습니다.
- `MainMenuController`와 `PauseMenuController`가 각각 따로 만들던 키가이드 패널을 `KeyGuideFactory`로 통합했습니다.
- 중복 리스너 등록을 막고, 오버레이는 비활성화 후 재사용하도록 정리했습니다.
- 결승선 랩 타임 디버깅 로그를 추가했습니다.
- `FinishLine.cs`와 `LapTimer.cs`에 트리거 진입, 체크포인트 판정, 랩 수락/거절 사유 로그를 넣었습니다.
- 출발 직후 FinishLine 선통과가 런을 종료시키던 문제를 완화했습니다.
- 체크포인트를 하나도 밟지 않은 상태에서는 경고를 숨기고, 일부만 밟은 경우에만 `Missing checkpoints.`를 표시하도록 바꿨습니다.
- `FinishLine.cs`에 `ResetRaceTimer()`를 추가해 새 시도 초기화 흐름을 정리했습니다.
- `CameraController.ResetCamera()`는 1인칭 앵커가 있으면 그 위치를 우선 사용하도록 수정했습니다.
- 인게임 노출 문자열은 영어로 통일해 TMP 글리프 누락 경고를 줄였습니다.

## 2026-05-19

- 랩 타임 시스템을 추가했습니다.
- 첫 가속 입력에서 타이머를 시작하고, 결승선 통과 시 체크포인트 완료 여부를 검사하도록 연결했습니다.
- 최근 기록과 Best 3 목록을 세션 단위로 유지하도록 정리했습니다.
- `LapTimeDisplay`로 Current, Recent, Best 3를 좌상단 HUD에 표시했습니다.
- `SteeringIndicatorUI`의 NaN 경고를 방어 코드로 정리했습니다.
- `NaN`과 `Infinity` 값이 UI에 직접 들어가지 않도록 검사 로직을 추가했습니다.
- 체크포인트 시스템을 완료했습니다.
- 순서 검증, 방문 상태 갱신, 색상 피드백, 완료 여부 확인이 정상 동작하는 것을 확인했습니다.

## 2026-05-18

- 메인 메뉴 설정 패널의 첫 클릭 표시 문제를 해결했습니다.
- `SettingsPanel`의 활성화 방식과 `CanvasGroup` 상태를 정리해 첫 클릭에도 바로 열리도록 바꿨습니다.

## 2026-05-17

- 공통 로딩 화면을 추가했습니다.
- 메인 메뉴 시작과 인게임 복귀가 이제 비동기 로딩 화면을 거치도록 바뀌었습니다.
- 메인 메뉴와 일시정지 메뉴의 키 가이드 오버레이는 삭제 대신 비활성화로 정리했습니다.
- 로딩 화면은 `Time.timeScale`과 무관하게 동작하도록 구성했습니다.

## 2026-05-15

- 1인칭 카메라의 위치/회전 보간을 제거해 입력과 시점의 끊김을 줄였습니다.
- 3인칭 카메라는 `RigidbodyInterpolation.Interpolate`를 사용해 추적 끊김을 완화했습니다.
- 1인칭 카메라는 런타임에 앵커 자식으로 부모화해 고정했습니다.

## 주행 디버그 메모

- 출발 직후 FinishLine을 먼저 밟는 경우, 체크포인트가 전부 끝나기 전까지는 랩을 인정하지 않도록 처리해야 합니다.
- 결승선 박스나 차량 오브젝트의 Rigidbody 유무가 트리거 안정성에 영향을 줄 수 있습니다.
- 체크포인트 로그는 순서 검증이 어디서 막히는지 확인하는 가장 빠른 확인 수단입니다.
- 랩 타임 문제를 볼 때는 `LapTimer`, `CheckpointManager`, `FinishLine` 순서로 확인하면 원인 추적이 쉽습니다.

## 주행 디버그 정리 (from README)

### 발견한 문제
- W를 떼도 잠시 가속이 이어지는 것처럼 보임
- 일부 기어 상태에서 차량이 반대로 움직이는 것처럼 보임
- 로그상에서 브레이크와 엑셀 상태가 명확하게 분리되지 않음
- 최고속 부근에서 엑셀을 떼도 속도가 더 붙는 것처럼 보임

### 확인한 원인
1. 입력과 물리 처리가 섞여 있어서 토크가 남아 있는 것처럼 보였습니다.
2. WheelCollider와 차체 물리 세팅 때문에 속도가 더 붙는 구간이 있었습니다.
3. 단순 관성만이 아니라 WheelCollider의 회전 상태, 타이어 슬립, 트랙 물리가 함께 영향을 주고 있었습니다.

### 적용한 수정
1. W/S 입력을 분리해서 엑셀과 브레이크가 같은 축을 공유하지 않도록 정리했습니다.
2. 입력은 `Update()`에서 받고, 바퀴에 실제 힘을 주는 처리는 `FixedUpdate()`로 옮겼습니다.
3. 중립과 전진/후진 기어의 감속 성격을 분리해서, 기어 상태에 따라 저항이 다르게 걸리도록 바꿨습니다.
4. `Motor`, `BrakeTorque`, `Slope`, 바퀴별 RPM과 슬립 값을 콘솔에 찍어서, 코드 문제인지 물리 문제인지 구분할 수 있게 했습니다.
5. 차체 드래그와 감속 관련 값을 낮추고, 주행감이 너무 급하지 않게 다시 조정했습니다.
6. `linearDamping` 값을 함께 조정해서, 엑셀을 뗐을 때 차체가 너무 급하게 감속되는 문제를 완화했습니다.

### 감속 튜닝 메모
`linearDamping`은 Rigidbody가 직선으로 움직일 때 받는 감쇠값입니다.
- 값이 크면 차가 더 빨리 속도를 잃습니다.
- 값이 작으면 차가 더 오래 굴러갑니다.
- 이 프로젝트에서는 엑셀을 뗐을 때 감속이 너무 급하다는 문제가 있어서, `linearDamping`을 낮추는 방향으로 튜닝했습니다.

### 현재 디버그 기준
- `Motor: 0`인데 속도만 오른다: 차체 또는 바퀴 물리 영향
- 바퀴 RPM이 계속 높다: 접지/마찰/구름 저항 문제
- `forwardSlip` 또는 `sidewaysSlip`이 크다: 휠 슬립 문제
- 특정 구간에서만 속도가 오른다: 트랙 콜라이더 또는 노면 물리 문제

### 현재 기어 규칙
- `-1` = 후진 (`R`)
- `0` = 중립 (`N`)
- `1` 이상 = 전진 기어
- 다운시프트는 `N`을 거쳐 `R`로 내려감

### 최근 조정값
- `engineBrakeTorque`: `60` → `10`
- 기본 카메라 인칭: 1인칭 우선
- `mouseSensitivity`: `1`

### 현재 튜닝 방향
- 엑셀 해제 후 바로 멈추지 않고 자연스럽게 속도가 떨어지게 조정
- 중립은 더 오래 굴러가게 조정
- 브레이크는 강하게, 하지만 과하게 급정지하지 않게 조정

## 완료 작업 (summary moved from README)

- 1인칭/3인칭 카메라 전환 및 보정
- 기어별 최고 속도 및 토크 튜닝
- 메인 메뉴 및 키 가이드
- 씬 전환용 공통 로딩 화면
- 랩 타임 시스템(첫 가속 시 시작, 체크포인트 기반 완주 판정)
- 체크포인트 시스템 구현 및 검증
- UI: 속도, 기어, 랩 타임 표시

## 남은 작업 (summary moved from README)

- 메인 메뉴 배경 이미지/카메라 연결 문제 해결
- 설정 패널 내용 추가 (해상도, 키 설정 등)
- 랩 타임 장기 저장 방식(파일/PlayerPrefs) 구현
- 키 가이드 중복 표시 이슈(이미 개선됨)
- 중/장기 계획: 음향 적용, 여러 차량, 레이스 플로우 고도화
